var bootstrap = {
    startup: function(data, reason) {
        // No need to do anything here for Zotero 7
    },

    shutdown: function(data, reason) {
        // Clean up any persistent objects if necessary
    },

    install: function(data, reason) {},
    uninstall: function(data, reason) {}
};
